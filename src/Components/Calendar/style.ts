import { Text, styled } from "@ignite-ui/react";

export const CalenderContainer = styled("div", {});

export const CalenderHeader = styled("div", {});

export const CalenderTitle = styled(Text, {});

export const CalenderActions = styled("div", {});

export const CalenderBody = styled("table", {});

export const CalenderDay = styled("button", {});
